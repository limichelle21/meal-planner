<div class="row">
   <div class="col-md-8">
     <h1>Ingredient: <%= @ingredient.name %></h1>
     <% @ingredient.recipes.each do |recipe| %>
       <div>
         Recipe: <%= link_to recipe.name, recipe %>
       </div>
     <% end %>
   </div>
 </div>
